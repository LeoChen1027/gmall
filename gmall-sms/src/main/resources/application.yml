server:
  port: 8083
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guli_sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT
    username: root
    password: 84994800
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
  redis:
    host: localhost
mybatis-plus:
  mapper-locations: classpath:mapper/sms/*.xml  # 读取映射文件
  global-config:
    db-config:
      id-type: auto

